# Review Log

Append-only operational history. Không sửa hoặc xóa lịch sử đã ghi.

> File này ship TRỐNG có chủ đích: lịch sử review thuộc về từng implementation
> và được tích lũy qua vận hành thật. Mỗi lần review hoặc phát hiện Rot,
> append một dòng mới theo format dưới đây — không chỉnh sửa dòng cũ.

| Date | Scope | Findings | Action | Evidence | Reviewer |
|---|---|---|---|---|---|
| 2026-07-16 | Rules & Hooks | Promoted task contract, pre/post edit facts, deliver receipt, unattended/upgrade support | Implemented mechanical gates and lifecycle/install tests | tests/run_all.sh | Codex |
| 2026-07-16 | Tools/Runtime | Fixed stale re-init/pre-edit docs, session diff facts handoff, stage flag parsing, upgrade dry-run marker kind, adapter typo validation | Added install/lifecycle/docs regressions | tests/run_all.sh | Codex |
| 2026-07-25 | Codebase intelligence | Deep scout found generated-source ambiguity, unsafe source collapsing, stale trust, state symlink and RAM retention risks | Added provenance-aware canonicalization, streaming budgets, freshness/coverage contract, tests and full-parity roadmap | docs/codebase-memory-deep-scout.md | Codex |
| 2026-07-26 | Tools/Runtime | Dogfood release v1.12.0 phát hiện 2 bug: smoke test `test_guard_characterization` dispatch mọi mode CLI gồm 21 mode `mutates=True` vào OS run đang mở của repo thật (latent — run đã seal thì bị reject nên không ai thấy); `token-telemetry` phát số cumulative nhưng `cost_ledger_summary` cộng dồn nên chạy 2 lần là gấp đôi hóa đơn ($49,45 vs $12,09) | Route mode mutates sang bản copy sandbox + thêm regression test; ledger lấy snapshot mới nhất theo `window_start` và khai `superseded_token_snapshots` | tests/run_all.sh | Claude |
| 2026-07-26 | Runtime & tests | Hai lỗ quy trình: (1) `docs/` không nằm trong `dist-manifest` nên field consumer-facing doc ở đó là vô hình với consumer — đã tái diễn ngay trong cùng session với `superseded_token_snapshots`; (2) lệnh verify bắt buộc `tests/run_all.sh` tự sinh `.pytest_cache` mà artifact-janitor coi là artifact, làm thứ tự verify → control-plane-check không bao giờ xanh | Ratchet derive key runtime từ `cost_ledger_summary`+`budget_status` bind vào doc shipped (grandfather key cũ, allowlist chỉ co); redirect cache ra /tmp ở cả 2 call site pytest + test quét thay vì hardcode | tests/run_all.sh | Claude |
| 2026-07-26 | Installer | `rot/review-log.md` + `memory/lessons-learned.md` tự khai trong header là 'ship TRỐNG có chủ đích' nhưng ship `verbatim` kèm lịch sử vận hành của vendor; auto-log gate buộc append mỗi session nên payload consumer phình mãi và version không thể pin chúng | `stage.py` strip data row khi copy (`SHIP_EMPTY_LOGS` + `log_header_only`), repo vendor giữ lịch sử; manifest khai `ship_empty:true`; gate C10i kiểm cả hai chiều | tests/install/run-tests.sh | Claude |

| 2026-07-26 | Kernel surface | Bốn hệ con (Visual Review, Team orchestration, Codebase Intelligence, Discovery/Prototype) có 0 lần dùng trong 32 dogfood run; 19 verb mồ côi; 2/4 context mode chưa từng được chọn; 5 claim shipped sai — VALIDATION.md khai Team control plane 'đã kiểm chứng qua vận hành' khi TEAM_RUNS_DIR chưa từng tồn tại, quality-gates.md ship bảng gate sai ở mode=lean, context-loading.md giấu 2 task_signal hợp lệ | Gỡ 4 hệ con + 19 verb + 2 mode; verb 53->34, guard bundle 12140->9188 dòng, kernel md 6906->3332, manifest 122->85, full kernel 296863->147507 B; di dời 2 meter sang scripts/measure_budget.py; 8 suite xanh | docs/slim-baseline.md | Claude |
| 2026-07-26 | Docs & tests | Danh sách layer tồn tại 4 bản viết tay và đã lệch: README gộp Memory & Knowledge (7 dòng), PilothOS.md tách (8 dòng), và chính PilothOS.md tự mâu thuẫn — sơ đồ gộp còn bảng ngay dưới tách. 8 command file đều tự nhận SKILL.md là SSOT rồi vẫn chép lại danh sách bước, hai bản chép đã trôi khỏi nhau. 6 thư mục kernel chỉ chứa một README ba dòng lặp lại đúng ô mô tả ở index cha. production-review duyệt cây repo 2 lần cho một lần kiểm. | Mở rộng sync_docs.py để trích vùng có marker từ nguồn; bảng layer + đoạn cross-cutting sinh từ PilothOS.md và được D10 gác; xoá sơ đồ trùng ở 2 nơi; làm mỏng 8 command file về pointer; xoá 6 thư mục scaffolding (manifest 85->79); production-review lấy kết quả janitor/state-doctor từ control-plane-check thay vì chạy lại; enter_case gộp 32x3 dòng setup | tests/docs/run-tests.sh | Claude |
| 2026-07-26 | Doc contracts | 13 claim trong doc shipped không có gate nào kiểm. Nặng nhất: quality floor chỉ so KEY nên đặt route_confidence=0.5 trong evidence-routing.json vẫn qua toàn bộ suite; Layer Index Contract có mệnh đề thoát 'khi áp dụng' nên 5/10 index lặng lẽ không tuân thủ; RECEIPT_SEALS_WARN_LINES=500 tồn tại trong code và không được doc ở đâu; chiều ngược của command-table bị waive bằng câu '15 internal mode vắng mặt hợp lệ' — đúng 15 mode đó là chỗ 9 verb mồ côi ẩn mình. Phát hiện thêm: Evidence Router vẫn chấm điểm team và route sang mode team dù toàn bộ máy móc thực thi/kiểm chứng team đã bị gỡ ở Phase 2. | Thêm tests/unit/test_doc_contracts.py (14 gate parse doc thay vì hardcode); quality floor so giá trị; thu hẹp Layer Index Contract về 3 mục bắt buộc rồi gate; bật chiều ngược command-table với waiver list có gate riêng; D7 phủ adapters/claude; regex bảng token mở tới cột %; gỡ team routing khỏi router, giữ mandatory independent review. Mutation test 6/6 gate bắt được lỗi cố tình gieo. | tests/unit/test_doc_contracts.py | Claude |
| 2026-07-26 | Adapters | Ba adapter cursor/codex/antigravity ship kèm mọi bản cài nhưng không có hook, không exec surface, 0 test chạy task qua chúng, 0 run khai adapter non-claude — README vẫn quảng cáo 'Native adapter ✅' cho cả bốn cột. adapters/claude/rules/ stage vào .claude/rules/ mà Claude Code không có loader nào cho thư mục đó. adapters/claude/agents/README.md còn trỏ tới agent-teams/ và agents/team-roles/ đã xoá ở Phase 2. | Gỡ 3 adapter + adapters/claude/rules + skill pilothos-adapter + 2 command adapter; REMOVE_ALLOWLIST đóng hẳn nên remove_path chỉ còn chạm self-prune whitelist; ALLOWED_ADAPTERS={claude} nên plan xin adapter không ship bị reject thay vì im lặng cài rỗng; README đổi bảng 4 cột thành 2 cột kèm lý do; giữ nguyên phần Piloth phát hiện và tôn trọng .cursor/.codex của consumer | tests/install/run-tests.sh | Claude |
| 2026-07-27 | Consumer | Lần đầu đo Piloth ngoài repo Piloth: hai project VNG (vnggh-gio, vnggh-portal) cài v1.11.0 từ 07-19/07-24. Auto-log gate được dùng thật và được tuân thủ — 81 entry qua 5 ngày làm việc. OS control plane, hệ con lớn nhất kernel, được gọi 1 lần và 0 lần hoàn tất: 1 os-start, 7 os-evidence, 0 os-close, 0 receipt seal. Run duy nhất là task tích hợp API thật, chạy 2h21m, ghi đủ 7 bằng chứng verify (tsc/eslint/build/vitest 36/36), rồi dừng ngay trước bước viết receipt — state không có close_attempts nên os-close không bị reject, nó không được gọi. Hook-merge brownfield chạy đúng trên cả hai repo: mỗi event có 2-6 hook, hook consumer không bị ghi đè. | Ghi thành docs/field-report-first-real-use.md kèm phần 'điều này KHÔNG nói lên' (N=1, chưa có telemetry, đo trên v1.11.0 trước khi cắt). Chưa hành động theo phát hiện — cần hỏi người đã dùng vì sao không close trước khi thiết kế lại os-close | docs/field-report-first-real-use.md | Claude |
