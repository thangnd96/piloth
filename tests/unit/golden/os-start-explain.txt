{
  "fields": {
    "adapter": {
      "allowed": [
        "claude",
        "codex",
        "cursor",
        "antigravity"
      ],
      "note": "used for capability handshake; unknown adapters degrade explicitly",
      "required": false
    },
    "adapter_capabilities": {
      "note": "native|emulated|unavailable map; request values override the conservative adapter registry",
      "required": false
    },
    "affected_layers": {
      "default": "derived from paths",
      "note": "contract requires a non-empty list",
      "required": false
    },
    "budget": {
      "note": "object; budget.max_usd is an advisory cost ceiling",
      "required": false
    },
    "energy_budget_reason": {
      "note": "justify the blast radius of an expensive run",
      "required": "when expected_evidence names a full-suite/broad run"
    },
    "evidence_profile": {
      "allowed": [
        "code",
        "design_tokens",
        "docs",
        "generic",
        "release",
        "ui"
      ],
      "default": "generic",
      "required": false
    },
    "execution_strategy": {
      "default": "controlled_target if explicit target else repo_local",
      "required": false
    },
    "expected_evidence": {
      "default": [
        "manual verification receipt"
      ],
      "required": false
    },
    "intent": {
      "aliases": [
        "task_scope",
        "summary",
        "title"
      ],
      "default": "repo-local OS task",
      "note": "becomes contract.task_scope",
      "required": false
    },
    "locale": {
      "default": "en",
      "note": "localizes human-readable summary only; IDs and schema remain English",
      "required": false
    },
    "mode": {
      "aliases": [
        "os_mode",
        "piloth_mode"
      ],
      "allowed": [
        "standard",
        "strict"
      ],
      "default": "standard",
      "note": "strict adds gates; it does not change context loading",
      "required": false
    },
    "operational_preset": {
      "allowed": [
        "light",
        "standard",
        "strict"
      ],
      "required": false
    },
    "out_of_scope_paths": {
      "default": [],
      "required": false
    },
    "specialists": {
      "note": "consumer specialist registry entries; healthy qualified consumer entries outrank Piloth fallbacks",
      "required": false
    },
    "success_metrics": {
      "required": false
    },
    "target_footprint_policy": {
      "aliases": [
        "footprint_policy"
      ],
      "allowed": [
        "no_control_plane_files",
        "repo_local_state_allowed"
      ],
      "default": "no_control_plane_files if explicit target else repo_local_state_allowed",
      "required": false
    },
    "target_kind": {
      "allowed": [
        "external",
        "git",
        "non_git"
      ],
      "note": "cross-checked vs actual git detection",
      "required": false
    },
    "target_paths": {
      "aliases": [
        "allowed_paths",
        "affected_paths",
        "paths"
      ],
      "default": [
        "**/*"
      ],
      "note": "each must be a safe glob",
      "required": false
    },
    "target_repo": {
      "default": "<control-plane repo>",
      "note": "absolute path; must exist and not be inside pilothOS/memory/state",
      "required": false
    },
    "task_id": {
      "aliases": [
        "id",
        "request_id"
      ],
      "default": "task-<sha256[:12]> from intent",
      "required": false
    },
    "task_signal": {
      "allowed": [
        "API/backend",
        "UI/component",
        "architecture",
        "bug fix",
        "not_applicable",
        "release/deploy",
        "security",
        "tool/MCP"
      ],
      "default": "not_applicable",
      "required": false
    },
    "user_overrides": {
      "note": "rollout/execution override; safety review remains mandatory",
      "required": false
    },
    "work_packages": {
      "note": "independent work packages used by the scored team gate",
      "required": false
    }
  },
  "note": "All fields optional; a bare {} opens a repo-local run. See runtime/os-control-plane.md.",
  "result": "os_start_schema"
}
