# /pilothos-uninstall

1. Run `python3 pilothOS/scripts/pilothos_installer.py uninstall` — shows reverse-plan only.
2. Present plan; approval via AskUserQuestion (destructive — Governance requires it).
3. Run again with `--confirm`. Report result; ask explicitly before removing `pilothOS/` tree (default KEEP).
