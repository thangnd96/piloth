# Claude Native Subagents

No generic subagents are enabled by default. PilothOS defaults to single-agent
execution.

Create a native Claude subagent only after a real repeated need is validated and
logged — the same bar every other PilothOS artifact has to clear.
